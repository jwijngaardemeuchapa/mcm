# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri build artifacts (gigantesco — nunca commitar)
src-tauri/target/
src-tauri/gen/

# Releases e builds locais
src-tauri/*.rar
src-tauri/*.zip

# Claude Code memory (local apenas)
.claude/

# Lead Protocol — estado volátil por sessão (não compartilhado entre agentes)
.agents/local/

# Variáveis de ambiente (tokens, chaves de API — nunca commitar)
.env
.env.local
.env.*.local

# Arquivos de dados operacionais (não são código)
*.csv
*.xlsx
*.xls

# Documentos avulsos na raiz
TaskFlow.txt
FUP_Manager_Documentacao.md
"dashboard de BID.txt"
dashboard.csv
logo_meuchapa.png
